#include <stdio.h>
/* Ricevuti in input n numeri, dire se cՏ almeno una coppia di numeri uguali e mostrarli */

int main()
{
    int n, a;
    printf("Quanti numeri vuoi inserire?");
    scanf("%d", &n);

    for(int i; i <= n;){
        printf("Inserisci il numero: ");
        scanf("%d", &a);
        
    }
// non so come andare avanti
}